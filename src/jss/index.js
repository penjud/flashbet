import { makeStyles } from '@mui/material/styles';

const useStyles = makeStyles(() => ({
  container: {
    width: '100%',
    height: '100vh',
    overflowY: 'hidden',
  },
}));

export default useStyles;
